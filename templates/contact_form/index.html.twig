{% extends 'base.html.twig' %}

{% block body %}
    <div class="container main">
      {{ form_start(contact_form) }}
      {{ form_widget(contact_form) }}
      <button type="submit" name="button" class='btn btn-success'>Book Your Call</button>
      {{ form_end(contact_form) }}
    </div>
{% endblock %}
